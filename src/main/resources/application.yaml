spring:
  datasource:
    url: jdbc:mysql://localhost:3306/toDo_List?useSSL=false&serverTimezone=UTC
    username: root
    password: "root"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.MySQL8Dialect
    show-sql: true

  flyway:
    enabled: true
